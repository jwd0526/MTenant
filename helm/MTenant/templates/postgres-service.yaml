apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- range $key, $val := .Values.postgres.labels.service }}
    {{ $key }}: {{ $val }}
    {{- end }}
spec:
  type: ClusterIP
  ports:
  - port: 5432
  selector:
    app: postgres