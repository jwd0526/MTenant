apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mtenant.labels.postgres" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: 5432
  selector:
    app: postgres
