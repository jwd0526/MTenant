apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mtenant.labels.redis" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: 6379
  selector:
    app: redis
